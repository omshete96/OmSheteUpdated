{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
-       "distDir": "dist/public"
+       "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
